"use client";
import { FormEvent, useState } from "react";
export function PasswordChangeForm() {
  const [notice, setNotice] = useState(""); const [submitting, setSubmitting] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); const newPassword = String(data.get("newPassword") ?? ""); if (newPassword !== String(data.get("confirmPassword") ?? "")) { setNotice("새 비밀번호 확인이 일치하지 않습니다."); return; } setSubmitting(true); setNotice(""); const response = await fetch("/api/member/password", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentPassword: data.get("currentPassword"), newPassword }) }); const result = (await response.json()) as { error?: string }; setNotice(response.ok ? "비밀번호를 변경하고 다른 기기의 로그인을 종료했습니다." : result.error ?? "변경하지 못했습니다."); if (response.ok) form.reset(); setSubmitting(false); }
  return <form className="request-form member-profile-form" onSubmit={submit}><label><span>현재 비밀번호</span><input name="currentPassword" type="password" autoComplete="current-password" required /></label><div className="request-form-grid"><label><span>새 비밀번호</span><input name="newPassword" type="password" minLength={10} maxLength={128} autoComplete="new-password" required /></label><label><span>새 비밀번호 확인</span><input name="confirmPassword" type="password" minLength={10} maxLength={128} autoComplete="new-password" required /></label></div><button className="primary-link request-submit" type="submit" disabled={submitting}>{submitting ? "변경 중…" : "비밀번호 변경"}</button>{notice && <p className="member-auth-notice" role="status">{notice}</p>}</form>;
}
